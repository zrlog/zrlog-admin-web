upstream zrlog-admin-backend {
    server localhost:17080;
}

upstream zrlog-admin-frontend {
    server localhost:3000;
}

server {
    listen 3001;
    #页面
    location / {
        proxy_pass http://zrlog-admin-backend;
    }
    #后台
    location /admin {
        proxy_pass http://zrlog-admin-frontend;
    }
    #后端服务
    location ~ ^/(admin/plugins/|admin/template/|api/) {
        proxy_pass http://zrlog-admin-backend;
    }
    #开发调试
    location /sockjs-node {
        proxy_pass http://zrlog-admin-frontend;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }
}
